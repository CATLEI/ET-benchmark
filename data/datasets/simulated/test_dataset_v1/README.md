# Test Dataset v1

This is a test dataset migrated from the original `data/test_dataset/` directory.

## Files

- `raw/tilt_series.hspy` - Original tilt series data (migrated from `test_data.hspy`)
- `ground_truth/volume.hspy` - Ground truth 3D volume (migrated from `ground_truth.hspy`)
- `metadata.yaml` - Dataset metadata

## Notes

- This dataset was migrated to the new directory structure
- Quality metrics need to be calculated using `scripts/calculate_quality_metrics.py`
- Tilt angles file needs to be created if not available

